{
  "name": "factorio-calculator-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "tauri": "tauri",

    "prepare:frontend": "node scripts/prepare-frontend.mjs",
    "dev": "yarn prepare:frontend && tauri dev",
    "build": "yarn prepare:frontend && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  },
  "dependencies": {
    "@tauri-apps/plugin-store": "~2"
  }
}
